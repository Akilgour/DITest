@model IEnumerable<DITest.Models.SalesOrderItem>

@DateTime.Now.ToString("yyyy-MM-ddTHH:mmzzz")
<p>
    @Html.ActionLink("Create New", "Create")
</p>

@Ajax.ActionLink("Update Inner",
                            "ShowItemList",
                            new { saleOrderId = 1},
                            new AjaxOptions
                            {
                                UpdateTargetId = "itemsList",
                                InsertionMode = InsertionMode.Replace,
                                HttpMethod = "GET",
                            },
                            new { @class = "btn btn-primary" })


<button type="button" class="btn btn-primary" id="btnAdd" data-saleOrderId=1 data-toggle="modal" data-target="#dialogEdit">
    Add
</button>

<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.SaleOrderItemId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SaleOrderId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Cost)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Quantity)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.SaleOrderItemId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SaleOrderId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Cost)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Quantity)
        </td>
        <td>
            <button type="button" class="btn btn-primary" id="btnEdit" data-saleOrderId=@item.SaleOrderId data-toggle="modal" data-target="#dialogEdit">
                Edit
            </button>
            @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
        </td>
    </tr>
}

</table>
